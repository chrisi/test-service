apiVersion: v1
kind: ConfigMap
metadata:
  name: test-service-b-config
data:
  application.yaml: |
    spring:
      application:
        name: test-service-b
      mongodb:
        uri: mongodb://admin:latrop@mongodb.skunkworks:27017/test_service?authSource=admin
      remote:
        echo:
          url: http://test-service-a.airplus.svc.cluster.local:8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-service-b
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-service-b
  template:
    metadata:
      labels:
        app: test-service-b
    spec:
      containers:
        - name: test-service-b
          image: dockerhub.gtidev.net/airplus/test-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: app-config
              mountPath: /config/application.yaml
              subPath: application.yaml
          env:
            - name: SPRING_CONFIG_ADDITIONAL_LOCATION
              value: file:/config/
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
      volumes:
        - name: app-config
          configMap:
            name: test-service-b-config
---
apiVersion: v1
kind: Service
metadata:
  name: test-service-b
spec:
  selector:
    app: test-service-b
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test-service-b-tls
spec:
  secretName: test-service-b-tls
  issuerRef:
    kind: ClusterIssuer
    group: cert-manager.io
    name: letsencrypt
  dnsNames:
    - test-service-b.dev.gtidev.net
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: test-service-b
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`test-service-b.dev.gtidev.net`)
      kind: Rule
      services:
        - name: test-service-b
          port: 8080
  tls:
    secretName: test-service-b-tls
